### [pst-point-tool.html][低] calcAlgo冒頭のNON_EL生成をモジュールスコープに移動（2026-06-13）

対象ファイル:
- `pst-point-tool.html`

**[低] NON_EL配列をモジュールスコープに移動**
- `const NON_EL = FILL_ORDER.map(id => BY_ID[id])` を `calcAlgo` 内から L317 モジュールスコープへ移動
- `calcAlgo` 内の同行を削除（計算ボタン押下ごとの再生成をなくす）

テスト: Playwright headless → 計算結果表示正常 PASS

---

### [pst-point-tool.html][高] state/maxStateとDOM入力欄の二重SSOT解消（2026-06-13）

対象ファイル:
- `pst-point-tool.html`

**[高] グローバル変数を appState オブジェクトに統合**
- `T, C, P0, state, maxState, feasible` を `const appState = { target, current, initialProgress, counts, maxCounts, feasible }` に統合（L348）
- `sumState()` / `doCalc()` を `appState` 参照に更新

テスト: Playwright headless → 全ケース PASS

---

### [pst-point-tool.html][高] render()の多責務分離（SRP違反）（2026-06-13）

対象ファイル:
- `pst-point-tool.html`

**[高] render() を 3 層に分割**
- `buildViewModel()` (純粋関数 → `{ kind: 'infeasible'|'achieved'|'rows', ... }`)
- `_renderRows(vm)` / `_renderSummary(vm)` / `_renderOrder(vm)` に DOM 描画を分離
- `renderAll()` がこれらを統括

テスト: Playwright headless → 全ケース PASS

---

### [pst-point-tool.html][高] adj()での全行innerHTML再構築を差分更新に変更（2026-06-13）

対象ファイル:
- `pst-point-tool.html`

**[高] count→0 時に行を非表示（display:none）で対応**
- `_renderRows()` 内の各行に `id="row-{id}"` を付与
- `adj()` で count=0 時は `row.style.display = 'none'` に変更（全行 innerHTML 再生成を回避）
- count=0 時も `cv.textContent` を先に更新するよう adj() 順序を調整

テスト: Playwright headless → −ボタン操作後カウント 0 更新・行非表示確認 PASS

---

### [pst-point-tool.html][高] renderOrder連打時の再実行をrAFで集約（2026-06-13）

対象ファイル:
- `pst-point-tool.html`

**[高] requestAnimationFrame で renderOrder を1フレーム1回に間引く**
- `let _orderRaf = 0` と `scheduleOrder()` 関数を追加
- `adj()` 内の `renderOrder()` 呼び出しを `scheduleOrder()` に置換

テスト: Playwright headless → ±連打後 order 正常表示 PASS

---

### [pst-point-tool.html][中] onclick文字列をイベント委譲+data属性に変更（2026-06-13）

対象ファイル:
- `pst-point-tool.html`

**[中] 動的ボタンの onclick を data 属性 + イベント委譲に変更**
- `_renderRows()` 内の `onclick="adj(...)"` を `data-id` / `data-d` 属性に変更
- `result-rows` に `addEventListener('click', ...)` でイベント委譲リスナーを追加
- HTML 静的ボタン (`btn-calc`, `spec-toggle`) も `addEventListener` 化し、inline handler を完全除去

テスト: Playwright headless → onclick= が 0 件であることを確認・動作 PASS

---

### [pst-point-tool.html][低] CSPメタタグ追加（2026-06-13）

対象ファイル:
- `pst-point-tool.html`

**[低] Content-Security-Policy メタタグを追加**
- `<head>` に CSP meta を追加（L5）
- `script-src 'self' 'unsafe-inline'`（インライン script ブロックが存在するため 'unsafe-inline' 追加）
- 外部スクリプト読み込みを禁止し多層防御を強化

テスト: Playwright headless → コンソール CSP 違反エラーなし PASS
残課題: 外部 JS ファイル化後に 'unsafe-inline' を除去すれば完全な script-src 'self' が設定可能
